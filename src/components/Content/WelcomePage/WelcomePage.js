import React from 'react'
import { withRouter } from "react-router";

const WelcomePage = () => {
    return (
        <h2>Welcome to Memory Book v1.0</h2>
    )
};

export default withRouter(WelcomePage);
